# Next Step Plan: Paper Rewrite After Mechanization

## Goal

Implement Phase 10 of `mechanization_plan.md`: revise
`1_coherence_depth.tex` so its mechanization claims exactly match the completed
fixed raw extension-calculus bridge, active-basis package, sparse/full
recurrence package, and case-study audit.

## Files To Update

- `1_coherence_depth.tex`
- `mechanization_plan.md` after the rewrite is verified
- `next_steps.md` after the rewrite is complete

## Preparation

1. Search `1_coherence_depth.tex` for broad mechanization claims:
   - "paper-level bridge"
   - "reasonable presentations"
   - "Agda programs"
   - "elaboration"
   - "parser"
   - "Fibonacci"
   - "mechanization"
2. Read the mechanization table and nearby prose before editing.
   - Preserve claims that are already exactly supported.
   - Tighten claims that sound like arbitrary Cubical Agda elaboration.
3. Cross-check every module named in the paper against:
   - `docs/theorem_index.md`
   - `scripts/check_coherence_depth_artifact.sh`
   - `agda/Test/MetatheorySmoke.agda`

## Edits To Make

1. Replace any "paper-level bridge" phrasing with a precise claim:
   a mechanized bridge for the fixed raw extension calculus.
2. Add the limitation explicitly wherever the reader might infer more:
   this is not a parser or elaboration theorem for arbitrary Cubical Agda
   programs.
3. Update the mechanization table to include only checked artifacts:
   - raw syntax and admissibility:
     `RawStructuralSyntax.agda`, `RawStructuralTyping.agda`
   - canonical trace and `mu` invariance:
     `CanonicalTelescope.agda`, `TraceCostNormalForm.agda`,
     `PresentationEquivalence.agda`, `MuInvariance.agda`
   - surface bridge and horn image:
     `SurfaceNormalizationBridge.agda`, `SurfaceToHornImage.agda`
   - active-basis naturality:
     `FiniteInterfaceBasis.agda`, `GlobalActionSemantics.agda`,
     `ActiveBasisContract.agda`
   - sparse/full recurrence:
     `SparseDependencyRecurrence.agda`, `FullCouplingEnvelope.agda`
   - case studies and audit data:
     `CaseStudies/*.agda`, `runs/coherence_depth_case_studies/*.yaml`,
     `scripts/coherence_depth_audit.py`
4. Replace broad "reasonable presentations" language with:
   presentations generated by the explicit `PresentationStep` constructors.
5. Present the Fibonacci law as the fully coupled endpoint of the sparse
   dependency calculus, not as a law for every sparse or orthogonal extension.
6. Confirm theorem names in the prose still appear in `docs/theorem_index.md`.

## Verification

```bash
latexmk -pdf 1_coherence_depth.tex
./scripts/check_coherence_depth_artifact.sh
```

If `latexmk` is unavailable, record the failed command and run the strongest
available LaTeX fallback in the plan. If the paper names a missing or
unchecked Agda module, either update the paper to an existing checked module or
record the mismatch in `mechanization_plan.md`.

## Completion Updates

After successful verification:

- remove Phase 10 as an open item from `mechanization_plan.md`
- record any remaining non-Agda caveats, especially LaTeX tooling caveats
- replace `next_steps.md` with the next concrete post-rewrite task or a short
  "no remaining mechanization steps" note
